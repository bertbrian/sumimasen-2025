export type Variant = "cool" | "dotted" | "ultrathin" | "ultrasafe" | "delay";

// This ensures EVERY Variant is accounted for exactly once
// export type VariantMap = Record<Variant, VariantDetail>;
